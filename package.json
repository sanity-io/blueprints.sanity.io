{
  "name": "@sanity/blueprints-jsonschemas",
  "version": "0.1.0",
  "description": "JSON schemas for Sanity Blueprints configuration files",
  "main": "./schemas.js",
  "files": [
    "schemas.js",
    "pages/schemas",
    "README.md"
  ],
  "type": "module",
  "keywords": [
    "sanity",
    "blueprints",
    "json-schema",
    "validation"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/sanity-io/blueprints.sanity.io.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.19 <22 || >=22.12"
  },
  "arc": {
    "app": "blueprints",
    "http": {
      "/*": {
        "method": "any",
        "src": "./"
      }
    }
  },
  "scripts": {
    "start": "sandbox --quiet",
    "test": "node --test tests",
    "test:watch": "node --test --watch tests",
    "test:coverage": "node --test --experimental-test-coverage tests",
    "test:unit": "node --test tests",
    "test:integration": "node --test tests/integration",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prepublishOnly": "npm test && npm run lint",
    "release": "./scripts/release.sh"
  },
  "devDependencies": {
    "@architect/functions": "^8.1.9",
    "@enhance/ssr": "^4.0.3",
    "@paramour/css": "^1.0.0-rc.2",
    "parse5": "^8.0.0",
    "prismjs": "^1.30.0",
    "@architect/deploy": "^5.0.8",
    "@architect/sandbox": "^7.1.0",
    "eslint": "^9.32.0"
  }
}
