{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://blueprints.sanity.io/schemas/2024-10-01/sanity.function.https.json",
  "title": "sanity.function.https",
  "description": "A Sanity Function that is invoked asynchronously via Web Socket messages",
  "type": "object",
  "properties": {
    "type": { "enum": ["sanity.function.wss"] },
    "name": { "type": "string" },
    "src": { "type": "string" },
    "enabled": { "type": "boolean" },
    "memory": { "type": "number" },
    "timeout": { "type": "number" },
    "env": { "type": "object" }
  },
  "required": ["type", "name", "src"]
}
