{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://blueprints.sanity.io/schemas/2024-10-01/sanity.function.https.json",
  "title": "sanity.function.https",
  "description": "A Sanity Function that is invoked synchronously with an HTTP request that returns an HTTP response",
  "type": "object",
  "properties": {
    "type": { "enum": ["sanity.function.https"] },
    "name": { "type": "string" },
    "src": { "type": "string" },
    "enabled": { "type": "boolean" },
    "memory": { "type": "number" },
    "timeout": { "type": "number" },
    "env": { "type": "object" }
  },
  "required": ["type", "name", "src"]
}
